# Legacy baseline corpus

These CSV files are retained only as a reproducible performance and correctness
corpus. They do not define 2.x configuration or behavior.

Milestone 3 records a manifest, source commit, environment, and 1.x measurements
under `docs/benchmarks/`. A deterministic 2.x fixture generator may replace
this corpus after the baseline is preserved.
