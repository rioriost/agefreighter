# B09 / B10 / B12 supervised continuation

September23,2026. User requested Astra xhigh supervision and multiple Astra high
implementation agents. Existing base-route qualification9/9 and extended ledger
6pass/6partial remain unchanged until new acceptance evidence is collected.

## Work allocation and concurrency

- Astra xhigh supervisor: independent design/safety review, then integrated
  implementation and evidence review. No direct cloud or shared GUI mutations.
- Astra high B09: readiness-control removal admission/panel/tests.
- Astra high B10: store crash-lock identity, explicit recovery/panel/tests.
- Astra high B12: canonical-import rejection evidence and transport test coverage.
- Root coordinator: shared entrypoint registration, integration, serial host/GUI
  testing, redacted evidence, exact-scope approval and resource safety.

The supervisor's initial review finishes before all three implementation workers
run concurrently. It resumes for final review after worker slots become free.
Each worker owns separate source files. Nobody modifies accepted operator records,
credentials, Azure resources or the normal VS Code profile during local work.
Only root runs actual Extension Host/GUI tests to avoid shared-desktop collisions.

## B09: controlled readiness-record lifecycle

Implement a native already-running-VM path with independently matched fresh
successful readiness, same boot/artifact, idle health, exact ownership, no
current/history references, archive-before-delete and single-use intent. Preserve
GET-only reconciliation after uncertain replies; never replay DELETE.

Historical Pending instance views with missing output/timestamps remain blocked.
The five manually approved removals earlier today are not native lifecycle
qualification. A dedicated same-boot old-readiness/current-readiness pair is a
possible later native trial; do not change old qualified records to fabricate it.
Remaining non-lifecycle B09 surfaces/faults must be enumerated separately rather
than declaring the entire branch passed from one successful deletion.

## B10: explicit crash-lock recovery

New locks record durable versioned PID and same-OS-boot identity before entering
the protected action. Explicit recovery must require a definitively dead owner,
unchanged lock/record identity, bounded preview and durable evidence preservation.
Ordinary acquisition and recovery share serialization; replacement locks must
never be unlinked. Legacy empty, malformed, foreign-boot, live or uncertain-owner
locks remain blocked. Recovery does not reconnect, dispatch or replay cloud work.

Unit/temp-store and real isolated-host crash tests are distinct from a signed-in
active Azure operation. Any later real crash targets only the approved extension
host/operation, not the whole user's VS Code process or unrelated editors.

## B12: rejected verification stays rejected

Current canonical checks already reject the inspected invalid reports. Existing
tests intentionally retain hash-valid rejected JSON as failure evidence; no
false-PASS bug is asserted. Preserve failures and classify any rejected archive
explicitly if changing publication. Wrong identity/profile/coverage/hash/length,
malformed JSON and transport failure must not create a PASS tab, accepted result
or automatic redownload/replay.

Possible bounded live follow-up: authentic read-only report GET with wrong
expected seal/length in a disposable test store, preserving the actual guest
manifest separately. This proves transport rejection only unless actual signed-in
production GUI entrypoint is exercised. New semantically invalid test blobs need
their own approved scope; accepted reports are never overwritten for tests.

## Integration and live gates

1. Worker targeted tests and xhigh design review.
2. Root full unit/type/build/CLI contracts and serial isolated real-host checks.
3. Xhigh integrated-diff review; fix material findings and rerun affected tests.
4. Package an identified candidate and obtain required action-time install approval.
5. Scope dedicated live cases with exact operations, expected refusals, account,
   immutable evidence, cost/time bounds, stop behavior and required approvals.
6. Observe actual outcomes, retain failure/positive controls, update only proven
   ledger cells, and stop exact compute immediately after useful work.

No new live fault-test authority is inferred from the completed B03 session.
All B09/B10/B12 classifications remain partial pending their required live proof.

## Integrated local result, September23 09:23UTC

All three Astra high implementations were integrated. Astra xhigh found one
blocking B10 contention race: a duplicate-window acquisition could briefly hold
the gate while the original owner released its lock. The release now waits a
bounded two seconds without reclaiming an uncertain gate; its deterministic
regression passes. Supervisor re-review found no remaining blocking production
source finding. The new native local-recovery command is registered and documented.

- Final full unit suite: **608/608**, zero failures/skips; typecheck and build PASS.
- Actual locally built Go CLI source contracts: **14/14 PASS**. An initial
  invocation omitted the required test-binary setting and failed before running
  any contract; building and supplying the explicit test CLI resolved setup.
- An integration mock initially lacked the new command module; corrected the
  inert import map and reran its23cases and the full suite successfully.
- Actual isolated VS Code **1.139.0 arm64**: **25/25 PASS**. Twelve retained P1
  cases include eleven refusals; invalid cases create no new PASS tab and preserve
  record/report bytes. These call production code with synthetic ARM evidence.
- Two actual isolated Extension Host SIGKILL/recovery pairs PASS. Parent VS Code
  logs confirm exact killed PIDs; reopened hosts have different PIDs. Both preserve
  archives/intents, reject automatic lock acquisition, then exercise cancellation
  and explicitly scripted production-store lock recovery with no remote replay.
  This is not an actual operator modal click or signed-in active Azure failure.
- All **91** operator-store files are unchanged before/after the entire batch;
  aggregate SHA-256 `794d6a7e3c88b12a2b70b0373064bd40f2c746318104d0c82697ea8741551709`.
  Normal installed extension, credentials and Azure resources were not changed.

See [machine-readable local evidence](evidence/b09-b10-b12-local-20260923.json),
[B09 admission](b09-running-control-admission-20260923.md),
[B10 recovery](b10-lock-recovery-20260923.md), and
[B12 import boundaries](b12-rejected-import-preparation-20260923.md).
Base routes remain9/9; extended branches remain6pass/6partial. No native cloud
acceptance is inferred from this batch. Next is candidate installation approval
and separately bounded live cases; do not restart the completed B03 session.
