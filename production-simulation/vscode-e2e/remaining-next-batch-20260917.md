# Remaining branch qualification — September 17 execution batch

The user requested an inventory and authorized starting feasible remaining
work. The authoritative case status remains [the ledger](remaining-validation.md).
The nine base routes, B08 and defined B11 faults are complete; ten ledger rows
remain open. This batch does not reopen accepted graph fixtures.

## Order and acceptance

| Priority | Cases | Next bounded work | Required evidence before closure |
|---|---|---|---|
| 1 | B12 | Exercise production P1 import controller with invalid reports; then signed-in candidate display/import retest | Never persist/display new PASS on invalid evidence; retain failures; distinguish synthetic transport from live GUI |
| 2 | B01/B02/B04/B07 | Index retained source/runner/target/resize evidence and audit missing GUI choices | Exact default/override/mapping selections; unchanged VM identity across resize; no cloud writes for invalid choices |
| 3 | B06/B09 | Inventory actual approval surfaces and Cosmos fixed authentication; exercise denial, propagation, stale preview and duplicate controls | Account-scoped managed-identity read access; no implicit retry or unauthorized write; distinguish ARM role existence from data-plane readiness |
| 4 | B05/B10 | Prepare equivalent frozen P1 Gremlin representation and a fresh isolated GUI migration; combine planned window reloads during assessment/load/verification | Full 64-range canonical equality; each saved operation reconciles without replay after actual host reload |
| 5 | B03 | Exercise unrepresented other-cloud GUI selections with endpoint-only sources | No source ARM discovery, full migration and canonical equality; simulated other-cloud placement must not be called real third-party-cloud qualification |

Live work must first refresh ownership, resource state, billing/conservative
reserve, time, quota and health. The ceiling remains USD800 and outer deadline
2026-09-20T07:14:35.311Z, not a fresh grant. Current billing was previously
unavailable; do not infer remaining actual funds from the planning reserve.
Do not start all paths concurrently or resize accepted sources for coverage.
New sensitive-access or unpublished-artifact GUI confirmations remain subject
to their action-time approval requirements. Preserve all prior evidence.

## B12 production-controller batch completed

`p1QualificationPanel.test.ts` loads the actual `qualifyP1` controller with
inert VS Code, store and ARM adapters. It uses the production report downloader,
manifest/hash checks and canonical parser with synthetic HTTP responses. No
network requests, credential access, retained GUI-state edits or installed
extension changes occur. The test fixture is the frozen canonical P1 manifest,
not a newly measured target.

Twenty cases:

- Positive complete report: exact-byte retention, canonical verification,
  then persisted PASS and script-disabled success panel. Reopening rechecks
  stored evidence without another download.
- Eleven invalid documents: foreign envelope/target job; missing, duplicated
  or reordered ranges; forged matching leaf/summary claims; wrong fixture root;
  unsupported canonical version; incorrect total; comparison failure; missing
  read-only guarantee. No success persist/panel, one GET only. Hash-valid
  rejected documents remain available as evidence.
- Five transfer failures: hash, length, truncated JSON, HTTP403 and transport
  exception. No retention, success persist/panel or automatic retry.
- Changed retained job/command identity: rejected before any download.
- Previously passing but hash-corrupted stored bytes: rejected on reopen,
  no success panel, redownload or rewritten saved record.

Review: the adapters replace only environmental boundaries; the verdict,
download verifier and controller are production functions. No synthetic
document is inserted in the operator's actual store. The rejection assertions
check both rendered success and durable writes, not only thrown errors. These
are controller regressions, not signed-in GUI or live Azure fault evidence.
B12 therefore remains **partial**, with installed-candidate interaction still
to perform. No production behavior was changed in this batch.

Validation: TypeScript typecheck PASS; full extension unit suite **255/255
PASS** (235 existing plus 20 new). No tests skipped. No new live-cloud result
or installed Extension Host run is claimed by this batch.
