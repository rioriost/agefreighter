# B06/B07 fresh bounded trial — approval proposal

September 24, 2026. **Prepared only: this new scope is not approved, started or
an existing permission grant.** The normal GUI saved workflow
`b425c32a-3ba3-44bc-8415-a48114993c5c` at `05:19:31.788Z`. Its initial retained
draft is effect-free. Root has now completed normal GUI Review of all 18 source
mappings while the workflow remains `draft`; the reviewed source matches the
old mapping specification except for the new local name/target-graph fields.
No new source cloud action, runtime identity, approval or clock was created.
This initial Review does not replace the later two-panel review required after
inventory import and before target creation.
This document proposes one concrete new trial, not an extension of the old one.

The previous scope expired at `2026-09-24T04:18:14Z`; its guard recorded compute
stopped at approximately `02:11:50Z`. Its supplemental `before-02` was **not
executed**. The original failed observer remains inconclusive and consumed;
it does not prove a Cosmos denial or zero queries. Old resource cleanup is now
complete: [independent evidence](evidence/b02-b06-b07-trial-cleanup-20260924.json)
confirms all 21 identities absent, no old-account EventGrid derivative and the
original 93 operator files unchanged. Do not restart its VM, reuse its approval/clock,
or discard original receipts. Preserve B02 PASS and the actual B06 Restricted
Mode pre-dialog refusal PASS; neither needs another trial merely to retain it.

## Exact new resources and permissions proposed

Subscription `67c417f3-5a13-446c-afb9-40cd87f2fdb7`, existing resource group
`rg-af-vscode-p1-20260905-a`, Japan East / zone 1. All new resources below use
that subscription/group unless their existing parent is explicitly stated.
The ARM prefix is
`/subscriptions/67c417f3-5a13-446c-afb9-40cd87f2fdb7/resourceGroups/rg-af-vscode-p1-20260905-a/providers/`.

| Resource / action | Exact proposed binding |
|---|---|
| Runner | `Microsoft.Compute/virtualMachines/af-b425c32a3ba344bc8415`; Linux `Standard_B2s_v2` then ordinary same-VM `Standard_D4s_v5` resize; system-assigned identity, no public IP |
| Runner disk/network | Persistent OS disk `af-b425c32a3ba344bc8415-os`; NIC and NSG `af-b425c32a3ba344bc8415`; existing `vnet-af-vscode-p1/subnets/runner`; preserve the shared VNet/subnet |
| Private PostgreSQL | `Microsoft.DBforPostgreSQL/flexibleServers/afpg-b425c32a3ba344bc8415`; version 18, `Standard_E8ds_v5`, **MemoryOptimized**, 128 GiB (`storageSizeGB:128`), HA Disabled, public access Disabled; normal `agefreighter` database and AGE configuration/restart/readiness |
| Target network | New `vnet-af-vscode-p1/subnets/afpg-b425c32a3ba344bc8415`, `10.246.26.0/24`; new private DNS zone `af-b425c32a3ba344bc8415.postgres.database.azure.com` and its `runner` VNet link |
| Transfer storage | New account `afb425c32a3ba344bc8415a4`, sole container `af-b425c32a-3ba3-44bc-8415-a48114993c5c`; account-scoped operator Blob Data Contributor and container-scoped new-VM Blob Data Reader, reconciled by exact normal assignment IDs |
| Conditional upload exception | Only that new transfer account, if the observed organization policy requires it: preserve ownership tags, set `SecurityControl=Ignore` and authenticated HTTPS `publicNetworkAccess=Enabled`; keep `allowBlobPublicAccess=false`, `allowSharedKeyAccess=false` and HTTPS-only enabled. No group/subscription policy or source-network change |
| Source | Existing `Microsoft.DocumentDB/databaseAccounts/afcosmosp120260907`, database `p1`, container `graph`, unchanged generated P1 fixture and all **18 explicit mappings**, reviewed through the normal GUI; preserve source public-network/local-key-auth restrictions and throughput |
| Cosmos Reader | One new account-scoped Cosmos Built-in Data Reader (`sqlRoleDefinitions/00000000-0000-0000-0000-000000000001`) for this new VM's original system principal. Actual principal/tenant and assignment UUID come from fresh VM GET and normal GUI review. It grants account-wide read permission; trial queries remain bounded to `p1/graph` |
| Empty layout fixture | `Microsoft.Compute/disks/af-b425c32a3ba344bc8415-b07`, new Empty 4 GiB `StandardSSD_LRS`, Japan East / zone 1, LUN 0; attach once and detach once, without formatting, mounting or source-data writes |
| Identity-change case | Remove and recreate only this new VM's system-assigned identity once, after other evidence is sealed. Retain the original reviewed principal and Reader assignment unchanged until cleanup; grant no role to the replacement principal |
| Deployment records | New runner `af-b425c32a3ba344bc8415`, target `afpg-b425c32a3ba344bc8415` and storage `afb425c32a3ba344bc8415a4-transfer` deployment records and exact owned children |

VM tags remain `application=agefreighter`, the exact workflow UUID and
`purpose=discovery-and-migration`. The target uses `purpose=migration-target`,
transfer storage `purpose=artifact-transfer`, and the empty disk
`purpose=b07-incompatible-layout`. No accepted workflow/resource or existing
source role is an authorized mutation target. No new B02 VNet or migration/load
is included. Target credentials remain in normal SecretStorage; no secret is
written to this document or the evidence archive.

Fresh read-only preparation retained 16 exact absence checks, including separate
parent evidence for child absences; `10.246.26.0/24` is inside the shared VNet
and does not overlap its current subnets. The complete EventGrid baseline has
26 topics across two pages and no reference to the new storage account. These
are point-in-time observations and must be rechecked immediately before use.

## Budget, quota and fresh clocks

Request **USD 10 incremental maximum**, inside the unchanged **USD 800 overall
ceiling**. Obtain the actual approval time and set an absolute deadline no later
than **approval + 4 hours**. Compute is bounded independently to **90 minutes
from the first VM creation intent**, including provisioning time. Start exact
VM deallocation and PostgreSQL stop at minute **80**, or earlier on completion,
failure, cancellation or the absolute deadline's ten-minute margin. No automatic
extension, new target creation or restart after the stop trigger.

Fresh subscription and official-retail GETs at `2026-09-24T05:13:22Z` found:

| Item | Actual evidence / retail USD |
|---|---|
| VM B2s_v2 | Zone 1 supported, no SKU restriction; Bsv2 quota 10/100; **$0.109/hour** |
| VM D4s_v5 | Zone 1 supported, no SKU restriction; DSv5 quota 72/100; **$0.248/hour**; regional compute quota 84/101 |
| Rejected old target candidate D4ds_v5 | PostgreSQL DDSv5 quota **62/64**, leaving **2 vCores**, below the required **4**; availability and price alone cannot pass admission |
| Proposed target E8ds_v5 | PostgreSQL 18, zone 1 and 128-GiB storage supported; requires **8 vCores**; EDSv5 quota 48/256 and PostgreSQL regional quota 110/196; **$1.200/hour** |
| New combined compute | Bound the runner at D4s_v5 plus E8 target for all 90 minutes: **$1.448/hour × 1.5 = $2.172**, rounded up to **$2.18** |
| Trial reserve | **$7.82** for storage, transactions, networking, source RU, policy-created services, taxes/uncertainty and shutdown delays; total planning envelope **$10** |

These are observed retail rates and planning amounts, not the final bill or a
metered spending guarantee. Current accumulated cost/reserve must also fit the
$800 ceiling. Repeat normal SKU/quota/price/network admission before submission;
no quota increase, existing-resource deletion to free quota, or silent SKU
substitution is proposed. Meter selection excludes Windows, Spot, Low Priority
and Cloud Services. See Microsoft's [Retail Prices API documentation](https://learn.microsoft.com/en-us/rest/api/cost-management/retail-prices/azure-retail-prices).

Cost Management initially returned 429 at 05:36 UTC; a separate manually
reviewed query succeeded at **05:41:34 UTC**. Its two qualification groups total
**USD 537.51733577924031** in delayed month-to-date pre-tax charges. Reserving
both the prior trial's full USD 10 envelope and the new trial's USD 10 gives
USD 557.51733577924031, leaving approximately USD 242.48 of planning headroom
under USD 800. This is delayed billing plus explicit reserves, not a live invoice
or a guarantee against unreported accrual. Receipt and calculation hashes are
`daa26c68156c8b609538f02904eec0eb243c44abc2338f5da34dd973972bcfcb` and
`a443d1b7d9077b3c91270808f7b7065bfd6b43c827ea7fba040e2eeef1942bab`.

The existing Cosmos account's fresh GET at 05:36 UTC still shows public access
Disabled, local authentication Disabled, zero IP rules and its approved private
endpoint. Installed extension bytes match the packaged candidate. The good
37,215,444-byte Linux archive was rehashed at 05:44 UTC; the negative B09 archive
is not part of this proposal.

A fresh private guard directory must contain only this workflow's newly approved
manifest and later first-intent marker. The reviewed manifest-driven guard code
can be copied unchanged; old claim/clock/intent/result files cannot be reused.
The guard must be independently verified before the first VM submission.

## Bounded observations and source operations

**B06 v2:** at most **two pre-grant observations across this whole new trial**
(initial plus one manually reviewed diagnostic follow-up), and at most **three
post-grant observations**. Each call has a 60-second bound, at most **two IMDS
requests** (resource identity and managed-identity token) and **one Cosmos POST**
with the fixed read-only query `SELECT TOP 1 VALUE 1 FROM c`. No continuation,
automatic retry, token/Authorization/source-value output or key fallback.
Maximum output is 4096 bytes. Queries use the exact new VM/principal and the
same existing `p1/graph` binding.

The exact new VM Run Command names are `af-b06-v2-before-01`,
`af-b06-v2-before-02`, `af-b06-v2-after-01`, `af-b06-v2-after-02`, and
`af-b06-v2-after-03`. Before-02 requires manual diagnosis/review of the first
outcome under this explicit allowance and uses the same fixed v2 observer.
Its allowance does not authorize a code, query, permission or deadline change.
Stop pre-grant observation after the
first attributable same-principal RBAC denial; stop post-grant observation after
the first recognized read success. Unknown dispatch/response is GET-only
reconciliation, never replay. A timeout, generic error or other 403 remains
inconclusive; do not infer zero requests or RBAC causality from it.

**Source inventories:** exactly up to **two** normally reviewed complete
inventories of the unchanged 18-mapping source, each within its existing
30-minute / 4-GiB / no-swap bound, each followed by actual export and
hash-verified report import. No third inventory or source write is included.

**B07 process observers:** at most two additional 60-second controls named
`af-b07-before-<actual-second-inventory-UUID>` and
`af-b07-after-<the-same-UUID>`. Bind workflow/VM/operation/boot/guest-config hash
from the real second operation. Read only bounded VM metadata, exact unit/state
and numeric process identity; no token, source query, worker signal, delay or
restart. Their matching active-worker receipts must bracket the actual GUI
resize refusal. They establish an active inventory, not an active migration.

Keep at most 25 retained managed Run Command definitions at a time and at most
60 created across the trial. Archive-first deletion may remove only exact
new-trial terminal controls proven no longer referenced; no active, unknown or
current control removal. Native watcher cancellation stops watching, not the
worker. Preserve already submitted work and reconcile the same operation.

## Required order to reach the real guards

1. Preserve old evidence and complete its separately authorized cleanup. Verify
   the installed candidate and good Linux archive seals, prepare the new normal
   transfer storage, then create this runner and obtain actual fresh readiness.
2. Establish B06's real before-grant denial; approve one exact normal GUI Reader
   assignment and reconcile it without replay. Establish the same-principal
   post-grant read, then complete inventory 1 and import its sealed report.
3. **Before target creation**, open two ordinary source panels A/B for this same
   workflow and genuinely Review the unchanged 18 mappings in both. Keep B
   untouched. Do not close/reload it or invoke report/storage/access actions
   that reset its reviewed state; source re-review is blocked once a target exists.
4. Provision the actual private target and AGE readiness, perform normal
   same-VM resize, and retain disk/NIC/identity/zone continuity. With no active
   worker, attach the sole empty fixture disk, obtain the real normal GUI
   incompatible-layout refusal, detach it and confirm the original layout.
5. From A approve inventory 2. Obtain before observer → actual normal GUI resize
   refusal → after observer, all bound to the same running unit/operation and
   process start identities. If it finishes too early, retain the inconclusive
   outcome; do not manufacture a running state or replay it.
6. Let inventory 2 finish and import/hash-seal its report through A. Finished
   without `reportSHA256` still counts as active and would mask the next guard.
   With no pending/unknown guest work, refresh readiness in the runner panel,
   rotate the new VM identity, and prove a different current principal. Within
   the readiness window, use B's existing sampled-read approval to reach the
   actual stale-reviewed-principal refusal. Retain the old approved role/binding.
   Earlier stale-health, lost-review, active-operation or target refusal is not
   this result. Never create a migration object, which would mask these guards.
7. Archive full evidence, stop compute and complete exact-owner cleanup. Aim to
   start inventory 2 by minute 45 so its full 30-minute bound leaves five minutes
   for the principal case before the minute-80 stop. If remaining authorized
   time is inadequate, retain partial results and stop; do not extend the clock.

## Exact cleanup and evidence retention

Before any deletion, verify the real private archive bytes/hash and bind a final
manifest to actual resource IDs, current/original principals and normal role
UUIDs. Preserve original old/new receipts, accepted artifacts and any unknown
outcomes. Require VM deallocated and PostgreSQL stopped before role revocation
or resource deletion. Remove only the new VM and owned controls, both owned
disks, NIC/NSG, target and exact children/subnet/DNS/link, new transfer container,
account, exact new role assignments and deployment records. Include policy-created
EventGrid derivatives only after complete pretrial absence and exact new-account
ownership/creation evidence; unexpected attachments stop cleanup.

Use a shared canonical per-resource DELETE intent journal across helper versions.
One explicit mutation invocation per intent; uncertain results permit only GET
reconciliation. Child `ParentResourceNotFound` requires independent exact parent
absence. Generic NotFound never proves arbitrary resource absence. No wildcard,
resource-group deletion, source mutation, accepted-resource change or old-VM restart.
Stop evidence and later complete deletion evidence are distinct outcomes.

## Prepared evidence and pinned helpers

Preparation is retained privately under
`production-simulation/work/b06-b07-next-scope-20260924/`; no credentials or raw
source rows are included here. Runtime principal, role UUID, operation, approval
and clock fields remain unbound until their actual ordinary responses exist.

| Artifact | SHA-256 |
|---|---|
| Installed normal extension bundle | `7a2aa5ddf13454ff6dab4d4a8ad2cb9a172842c9c4fbcc7b5d8e94d3f79d1d34` |
| Normal unpublished VSIX | `956f04d565007e28bd1785d208f35d5b6868a7ca52e64c899c1af5b718aa327a` |
| Good Linux archive, 2.4.0-dev.646f0d4b361a | `07ba1744b5daf7e226915c50d46fa5eda704949fc7d175e2b39a150813f41692` |
| Actual initial GUI draft copy | `67fc8b7ffe05945b748530340dc62e0fc7d5411df3fc52b6adb8c37a140f9e92` |
| Normal GUI reviewed 18-mapping draft | `dcce811615159173c763967bc66032953b101612483b949af3803476086fc8f3` |
| GUI source-preparation receipt | `46594e6963c2fd5c326b2787068c97f4470ef3c66fd934ae4bad8fae92271bac` |
| Fresh quota/retail summary | `ac3d0dfbe8f704b23a78eb80c070a28e3f695ee684886f3fecb985773a480204` |
| Complete fresh quota/retail file hash manifest | `95c0632851d792232dfab89134feb7f7e7b791a66de6ab853954d4ac8b0a9ccb` |
| Sixteen pretrial absence receipts manifest | `113825ba81354ebee2f521753a15798b1e1f44e62244bac69c308066aa9b2993` |
| Complete 26-topic EventGrid baseline | `529b9780ad660d8796b6075dce2ad5a28a2f15ae67c1d5a84e82cb1cd9f36137` |
| New-scope bound preparation receipt | `4af5af3b4ad56e09442815fa42bacb345592f6a14305e4b643455c60607c0844` |
| Manifest-driven stop guard | `2840a43bbdcaae75e41eb61ba5b23d92f630663c4a2e3bb14985ad1dcdb5e356` |
| New-VM GET-only lifecycle planner | `2f5a91d7087cc8b69826032ae5f411aa3597c667793a3b59b60ef983a92bfa6c` |
| New-VM once-only disk/identity executor | `ac16f83c0bf9ed203efda716eeb069daa2a5571898978876185a70bee6e9c11f` |
| New-workflow exact cleanup helper | `b968cc7c539efea4de90bed4b5889ac610c16afac8b020ff3f12a9388e7416fc` |
| B06 v2 observer | `796bc88bb947b3a8fa6077c6c477bc2d40b3476400a4bde59f00318670fc0f60` |
| B06 v2 local body builder | `170dfeee42a0f93ff21d85ef632750a8800166ebd18cb79fda4b39378257ee0d` |
| B06 v2 once-only dispatcher | `e70a7377a542afc9ef459adbbc8bad059c9de9f36183bc4f2e49d4a27e9d2f13` |
| New-workflow B07 observer | `b23a245463925fa7e4e04544fd5928e318349b897a04271cd9b6a3dcb19c3167` |
| Unchanged B07 local body builder | `7a95d7bd018a0ae4e8cdacc76dc3b4aa317f8f8bd81be310a95d34aff8164a27` |
| Unchanged B07 process-observer base | `0d35975ef67274123a481a57a37fccf6f5df8683ad02bf1ea62fa7ca5b170e37` |

B06 v2 has **13 independently passing offline cases**, including final generated
payloads with fake transports and opaque token handling. Rebound B07 final
payload/observer **13**, lifecycle planner **6** and unchanged guard **12** cases
also independently pass. The exact B06 dispatcher has **10** passing tests and
an additional independent actual-loader/all-five-bodies/guard check. The
lifecycle executor has **12** passing tests, including approval changes before
dispatch, no replay and predecessor-approval binding. The new cleanup helper
has **40** passing tests, independently repeated. It can clean up a VM left
with identity=None only with sealed actual creation, disable intent/reply,
subsequent None GET, first-intent and guard-clock evidence. It never enables
identity or restarts compute to perform cleanup.

These pinned preparations authorize no operation and do not close B06/B07.
Only the actual approval manifest, clock, VM principal and resulting operation
bindings remain to be populated from real future events. All cloud mutations
remain with root; the agents preparing this document performed none.
