# B02 / B06 / B07 bounded execution — September 24 JST

**Historical approved scope; expired September 24 at 04:18:14 UTC.** The guard
confirmed compute stopped at 02:11:50 UTC. B02 is complete for its finite scope;
B06/B07 remain partial. See the [current ledger](b06-b07-live-20260924.md) and
[fresh proposal](b06-b07-fresh-scope-20260924.md). The preparation below is retained
as originally proposed, not renewed execution authority. The original B09 resources are gone and its
approval is not reused. The supervisor coordinates three Astra high agents.

## Completed preparation

- Actual normal installed GUI unknown-zone VM selection PASS, including clearing
  an explicitly selected zone and disabling preview until renewed selection.
  [Observed evidence](b02-unknown-zone-gui-20260924.json) is attributed to the
  supervisor; all 93 previous operator files retained identical bytes/modes.
- Fresh actual Azure SKU metadata identifies Southeast Asia / zone 2 /
  Standard_D2s_v5 as unavailable for this subscription. Zone 1 is the control.
  [B02 scope and exact empty VNet body](b02-placement-scope-20260924.md).
- Resize now refuses unresolved asynchronous inventory/catalog workers even
  after their short ARM control has finished. Source authorization is rechecked
  after approval and across awaited admission/readiness/dispatch operations.
  A cancelled prewritten intent is retained and never replayed.
- Independent code reviews pass. The packaged candidate passes **757/757 unit
  tests**, typecheck, production build and Extension Host compilation. Native
  execution of this candidate is still pending.
- One normal GUI draft was created, with all 18 P1 mappings entered through the
  native form and independently matched to the accepted generated fixture.
  Existing accepted records were not edited. No runner, target, storage,
  permission assignment or source operation was created by that draft.

## Exact proposed scope

Subscription: `67c417f3-5a13-446c-afb9-40cd87f2fdb7`.
Resource group: `rg-af-vscode-p1-20260905-a`.
Workflow: `0f83d520-cd03-4792-8e38-3f146abfde39`.

| New resource or action | Bound scope |
|---|---|
| Runner | `af-0f83d520cd0347928e38`, Japan East / zone 1, B2s_v2 discovery then normal same-VM D4s_v5 resize; one private NIC/NSG and persistent OS disk, no public IP |
| Private target | `afpg-0f83d520cd0347928e38`, PostgreSQL 18, D4ds_v5 / GeneralPurpose / 128 GiB, HA disabled, public access disabled; no migration/load |
| Target network | New subnet `afpg-0f83d520cd0347928e38`, proposed `10.246.26.0/24`, inside existing `vnet-af-vscode-p1`; new DNS zone `af-0f83d520cd0347928e38.postgres.database.azure.com` and its `runner` link; recheck non-overlap before approval submission |
| Transfer storage | `af0f83d520cd0347928e383f`, sole workflow container, account-only user Blob Contributor and container-only VM Blob Reader; authenticated public HTTPS, anonymous and shared-key access disabled |
| Storage policy exception | Only this new account: `SecurityControl=Ignore` where the same observed organization policy requires it; no group/subscription policy change |
| Cosmos role | One new account-scoped Built-in Data Reader for this runner's original system-assigned principal on `afcosmosp120260907`; retain the exact actual role UUID and principal from the GUI review |
| Read-only source work | Existing synthetic P1 database `p1`, container `graph`, all 18 mappings; two normal complete inventories, up to one explicitly reviewed additional attempt within the same limits; no source writes or throughput/network changes |
| Data-plane observer | One pre-grant and at most three manually reviewed post-grant constant TOP 1 queries from the same VM principal; no automatic retry, secrets or source values in receipts |
| Active-worker observer | At most two 60-second controls on this exact new VM, bracketing the GUI resize refusal; inspect only IMDS resource identity, the exact operation state and process/service identity, without tokens, source queries or worker changes |
| Incompatible layout | New empty 4 GiB StandardSSD disk `af-0f83d520cd0347928e38-b07`, tagged `application=agefreighter`, this workflow and `purpose=b07-incompatible-layout`; attach at LUN 0 then detach after actual GUI refusal; never format/mount/write it |
| Changed principal | After other evidence is sealed, remove/recreate only this new VM's system-assigned identity; prove actual stale-principal refusal; grant no replacement role |
| SKU fixture | Empty, unpeered Southeast Asia VNet `af-b02-sku-20260924` and sole nondelegated `runner` subnet; no VM, public endpoint or peering |
| Local trust test | Empty disposable workspace and actual Restricted Mode, using only this new workflow; preserve other workspace trust settings |
| Cleanup | Archive evidence, stop and delete only new resources and their exact owned children/roles/deployment records; preserve every pre-existing VM, DB, disk, graph, source role and shared VNet |

New VM, PostgreSQL and storage names were independently observed absent during
preparation. The source still has public networking Disabled and local/key auth
disabled. The proposed target subnet is outside every current subnet prefix;
this is a point-in-time observation, not admission for a later stale plan.

## Budget, time and stop behavior

Request a new scope ending **four hours after approval**, including setup and
cleanup; compute runs at most **90 minutes from the first VM creation intent**.
Start VM deallocation and target stop at minute 80, or earlier on completion,
unexpected failure, cancellation or the absolute deadline's ten-minute margin. No automatic
extension or restart after the stop trigger. The exact VM/PG deadline monitor
must have its actual approved clock/seals verified before VM submission.

Incremental trial ceiling **USD 10**, inside the unchanged overall **USD 800**
ceiling. Fresh delayed Cost Management data at 2026-09-23 22:58:25 UTC totals
USD 511.61008604864544 across the two qualification resource groups; this is not
real-time spend or a final invoice. Current live price/preflight estimates plus
reserve must fit the USD 10 scope before compute approval. On failure to obtain
adequate current cost/health/ownership/quota evidence, stop before creation.

The official retail API was checked at 2026-09-23 23:33 UTC: Japan East Linux
B2s_v2 is USD 0.109/hour, Linux D4s_v5 is USD 0.248/hour, and PostgreSQL Flexible
Server D4ds_v5 is USD 0.488/hour for the entire four-vCore SKU. Bounding the
single VM at its larger size and the target for all 90 minutes gives USD 1.104
compute, rounded to USD 1.11 plus USD 8.89 reserve. This is an estimate, not a
metered spending cap or invoice; storage, network, source RU, taxes and stopping
delays use the reserve. Complete current price responses and selection evidence
are retained privately under `pricing/` (summary SHA-256
`be87da98350307b6c3b69ab1487c206307c2664cc5ca91dc438fbe78f4df5cca`).
See Microsoft's [retail price API documentation](https://learn.microsoft.com/en-us/rest/api/cost-management/retail-prices/azure-retail-prices).

Managed Run Commands remain bounded by the product's 25 retained definitions,
including completed controls.
At most 60 definitions across this trial; if capacity is approached, archive
and verify only completed, no-longer-referenced new-trial control receipts before
their exact single deletion. Never remove active/unknown/current controls or
the active inventory's initiating control. Preserve guest data and workers.
Use the normal progress notification's Cancel to stop automatic watching only,
then bounded explicit refreshes; this does not stop the guest worker. A refresh
can start another watcher, which may also be cancelled. Record any steps already
performed before cancellation; never claim the worker was cancelled or that
zero polling occurred merely because Cancel was selected.
There is no resource-group deletion or wildcard cleanup.

## Execution order and evidence limits

1. Install/reload the pinned normal candidate; inspect actual installed bytes
   and native appearance. Run the fresh read-only B02 SKU refusal/control with
   the separately approved empty VNet. Preserve the unknown-zone result above.
2. Prepare the pinned good Linux artifact and workflow storage normally, then
   create the one new runner and reconcile actual readiness. Perform actual
   Restricted Mode refusal before restoring the empty test workspace only.
3. Execute the one pre-grant observer. Require a causally attributable Cosmos
   RBAC denial. Grant the one reviewed Reader using the native GUI, reconcile
   that exact assignment, then prove same-principal data-plane success and a
   successful normal inventory/import. A generic 403 or ARM `ready` is not PASS.
4. Review the unchanged source in two normal source panels A/B **before target
   creation** and keep both. Create the actual private target, perform the
   normal same-VM resize, and verify its identity seal.
5. Attach the empty new disk; observe real GUI incompatible-layout refusal;
   detach it and recheck the original layout/readiness.
6. Start the second genuine inventory from A. While independent guest process
   evidence proves the worker active, observe normal GUI resize refusal. This
   is an active source-worker case, not an active migration/load claim.
7. Finish and import the second sealed report through A, leaving reviewed B
   untouched. Refresh readiness, rotate the new VM principal, and use B's
   existing sampled-assessment approval to prove the specific stale-principal
   refusal before any new guest/source request. No migration is created.
8. Preserve evidence, stop compute, freeze exact cleanup identities and verify
   each new resource absent. Recheck all previous operator artifacts unchanged.

See the finite [B06 plan](b06-live-plan-20260924.md) and
[B07 plan](b07-live-trial-plan-20260924.md), including the
[two active-worker observations](b07-active-worker-observer-20260924.md).
Real trust-before-entry refusal is
distinct from the simulated post-modal timing regressions. Before/after RBAC
read success does not imply that a propagation delay was observed. B02's
historical real quota refusal is the private-target/resize path; initial-runner
quota timing remains covered separately by local contracts.

## Pinned software and retained preparation

| Artifact | SHA-256 |
|---|---|
| Normal new VSIX | `956f04d565007e28bd1785d208f35d5b6868a7ca52e64c899c1af5b718aa327a` |
| New production extension bundle | `7a2aa5ddf13454ff6dab4d4a8ad2cb9a172842c9c4fbcc7b5d8e94d3f79d1d34` |
| Good Linux 2.4.0-dev.646f0d4b361a archive, 37,215,444 bytes | `07ba1744b5daf7e226915c50d46fa5eda704949fc7d175e2b39a150813f41692` |
| One-query B06 observer | `004ff881b502145832b4bd842f19bc926dc48d2150dd8365db244a9917a15b5e` |
| B07 exact-operation process observer | `4e49b3fb10a6566b648a1ccfb0c519cc78d34a99e2db3fa1f196073264429464` |
| Local-only B07 observation body builder | `7a95d7bd018a0ae4e8cdacc76dc3b4aa317f8f8bd81be310a95d34aff8164a27` |
| Unchanged process-observer dependency | `0d35975ef67274123a481a57a37fccf6f5df8683ad02bf1ea62fa7ca5b170e37` |
| B02 exact VNet request body | `c5515407bfeaded04020b43da38ad3e40b573d32f873a60b9ec9c15d654a13e6` |
| Exact VM/DB deadline guard | `2840a43bbdcaae75e41eb61ba5b23d92f630663c4a2e3bb14985ad1dcdb5e356` |
| Archive-first, exact-resource cleanup executor | `95ffc085555c3006b1c0cb87742a2d17ec347654d229146137b4e334beb7af6a` |

The currently installed old bundle remains `3108910e2933f73b1b1d0edf92458e7384e2008261469c56da2f19789366da67` until the new installation is approved.
Private preparations, source/draft seals, billing/metadata receipts and exact
stop/cleanup controls live in `production-simulation/work/b02-b06-b07-20260924/`.
No credentials/authentication caches are copied or committed. VM principal,
role UUIDs, approval timestamps and expiry are filled only from actual events,
not fabricated in advance. Preparation hashes are not execution results.

The B06 observer passes 10 offline cases, the B07 observer passes 9, and the
deadline/cleanup helpers pass 33. Their offline tests block actual network or
Azure execution. The cleanup executor requires the retained private archive's
actual file hash, fresh ownership/dependency checks and stopped compute before
its first deletion. Missing or ambiguous receipts stop progress without
replaying a mutation. These controls have not been exercised against the new
resources because those resources have not been created.
