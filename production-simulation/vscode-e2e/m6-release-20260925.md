# 2.4.0 M6 release execution — September 25

Status: GitHub v2.4.0 and Homebrew published and verified; authorized Azure and
local cleanup complete. Marketplace 2.4.0 was uploaded by the user in Safari
and is awaiting service verification; public availability is not yet claimed.
The user authorized completing M6 through release and removing unnecessary
Azure resources and local `agefreighter-*` worktrees. The existing bounded
Azure budget and deadline remain in effect. No authorization is inferred for
unrelated resources or unclassified user data.

## Qualification carried forward

The [current ledger](progress.md) retains nine base routes and twelve finite
extended branches passing. Each historical case remains tied to its original
build; this is not a claim that all cases were rerun on one final build.

Independent inspection matched the current production bundle and installed
extension used in the final B02/B06/B07 cases: 2,893,801 bytes, SHA-256
`7a2aa5ddf13454ff6dab4d4a8ad2cb9a172842c9c4fbcc7b5d8e94d3f79d1d34`.
The retained Linux development archive remains 37,215,444 bytes, SHA-256
`07ba1744b5daf7e226915c50d46fa5eda704949fc7d175e2b39a150813f41692`.
At the initial preflight, all 347 tracked files under `cmd`, `internal`, `pkg`,
`scripts`, plus the Go module files and Makefile, matched qualified commit
`646f0d4b361aec55fcc3f44cb689e06fd915d6e1`. A formal release embeds new version,
commit and build-date metadata and therefore is not expected to have the same
archive hash as that development build.

## Final compatibility corrections

- Build the extension before its unit tests so a clean checkout has the bundle
  needed by the native fixture. Keep compilation failure fatal.
- Use the declared minimum VS Code 1.105.0 in the release workflow, matching
  the extension manifest and ordinary Extension Host CI.
- Preserve the byte-sealed PostgreSQL fixture's LF line endings on Windows.
- Test macOS/POSIX-only qualification helpers on their supported platforms.
  On Windows, verify explicit refusal without weakening directory durability,
  no-replay or deletion protections. Windows interrupted-lock recovery lacks
  the required boot identity; durable directory synchronization is also not
  supported there. These restrictions are documented in the installed guide.
- Update current user guides and distinguish the old design checkpoints from
  implemented and qualified behavior.

Before these corrections, the latest hosted extension CI failed on OS-specific
fixture assumptions, a missing pre-test bundle and Windows durability refusal.
Local checks now include `make check` (format, vet, vulnerability checks,
workflow lint, unit/race tests, fuzz smoke and reproducible package self-check),
extension typechecking/unit tests, 25 actual isolated Extension Host cases on
VS Code 1.105.0, and a shipped-dependency audit with no vulnerabilities found.
The extension matrix subsequently passed at `61eeba2`, including all three
unit-test platforms, source contracts, Extension Host and VSIX packaging.
The final hosted matrix and release workflow remain required for later changes.

## M6 continuation: coverage and guest output bounds

The latest engine CI at `61eeba2` failed the existing 90% product coverage gate
at 86.3%; it was not an extension failure. The continuation preserves the 90%
threshold and restores the documented qualification-tool boundary: newly added
non-shipped `production-simulation` files are listed explicitly alongside the
older exclusions. Architecture tests now enforce the complete exact list and
reject product dependencies on excluded qualification packages.

Additional tests exercise complete source-only PostgreSQL and Neo4j inventory,
JSON/Markdown command output, malformed/close failures, runner admission,
durability, report seals, credential cleanup, migration/resume and read-only
target evidence. The target wire double uses verified TLS and loopback port
5432, fails explicitly on port collision, and is not live database qualification.

These tests found a production defect: embedding `bytes.Buffer` exposed
`ReadFrom`, allowing subprocess pipe copying to bypass the intended output
limit. A private named buffer now keeps all writes bounded. Regressions cover
both stdout and stderr, overflow failure evidence and refusal to replay.
Linux-native tests additionally cover journal exit status, missing or warning
evidence, active workflows, OOM counts and oversized journal output.
This is a Go runner correction after the historical live build, not a claim
that the old archive already contained the fix. The extension bundle and
connector/migration engine implementations are unchanged.

Local `GOFLAGS=-p=2 make check-full` passed with the pinned AGE, PostgreSQL 17,
Neo4j and PostgreSQL 19 Beta 3 services and 90.2% product coverage. An initial
unrestricted race run timed out in the existing SQL/PGQ corruption test;
two isolated repetitions and the complete bounded-concurrency run passed
without changing the timeout or suppressing any test. The full runner suite
also passed natively in an isolated Linux arm64 container. Hosted Linux x64
checks subsequently passed at `63d00da`, with 90.1% product coverage, followed
by the protected preparation and tagged release gates below.

The mutable PostgreSQL `19beta3` tag resolved to a different digest locally.
The harness refused it; validation used the original qualified digest directly,
without changing the pin or claiming qualification of the replacement image.

The user reconfirmed a USD5 incremental reserve for the exact retained release
CI VM, stopping at minute 110 with a 120-minute maximum, within the inherited
USD800 ceiling and September 28 12:45:06 UTC deadline. Fresh read-only checks
matched its original disk, NIC, tags and deallocated state. This approval does
not authorize resource deletion or permission changes.

## Published release and follow-up

PR [#27](https://github.com/rioriost/agefreighter/pull/27) passed all required
checks and was squash-merged through normal branch protection to
`a6ceedd4522ec49866c5f7225832330bb5ba4276`. The verified candidate and merged
commit have the same tree. Protected, non-publishing preparation
[36114778332](https://github.com/rioriost/agefreighter/actions/runs/36114778332)
passed quality/performance, live Cosmos, signing/notarization, native platform
execution, VSIX packaging, attestation and Homebrew preflight.

With explicit user approval, annotated tag `v2.4.0` was created at that exact
commit. Tagged run
[36115973959](https://github.com/rioriost/agefreighter/actions/runs/36115973959)
published the [release](https://github.com/rioriost/agefreighter/releases/tag/v2.4.0)
at 09:17:52 UTC and updated `rioriost/homebrew-cask`.

The first tagged Azure job reached PostgreSQL 19 during temporary bootstrap:
its earliest connections returned reset/EOF and SQLSTATE `57P03`, while later
application tests passed. A single failed-gates-only retry passed, including
the actual Cosmos gate; no required test was skipped, no gate bypassed and
the release tag was not moved. A post-release follow-up changes PostgreSQL
readiness to TCP loopback rather than the temporary Unix-socket server in
CI, release, Azure integration and local development scripts. Regression tests
execute all four workflow startup scripts against delayed/unavailable TCP
fixtures and check all three local probes. This workflow-only correction is
for subsequent executions, not part of the already-published tag.

All 16 public assets were downloaded. All six archive checksums and their
GitHub provenance passed verification against `refs/tags/v2.4.0`, the exact
source commit and the release workflow, with self-hosted attestors disallowed.
The public Homebrew Formula matches the release asset byte for byte.
Both public macOS arm64 executables passed signature verification and explicit
`notarized` code requirements and reported 2.4.0 with the expected commit.
Generic `spctl --type execute` rejected the standalone CLI as not an app;
that result is not substituted for Mach-O notarization evidence.

The published VSIX has ten members and retains the same qualified production
extension bundle. Its SHA-256 is
`f1ad34fa5a970194ee5d1dbeb298fbb9ad2231c6ee05dd09d0140fba156c37cf`.
Marketplace registration, authentication and verification remain separate
from this GitHub asset. After an account-access error, the user reported a
successful 2.3.0-to-2.4.0 upload in Safari at 21:13 JST; its state was
`Verifying`, not yet a confirmed public listing.

## Azure cleanup and retained CI infrastructure — complete

The existing CI VM was started once at 08:52:05 UTC under the reviewed
USD5 reserve and an independently running exact-VM stop guardian. Following
successful release gates and an idle-runner check, it was deallocated once.
The guardian and an independent Azure GET confirmed `PowerState/deallocated`
at approximately 09:18:18 UTC, before the original 10:42:05 UTC stop deadline.
GitHub subsequently reported the exact runner offline. Runtime was about
26 minutes 13 seconds; no VM, disk, NIC or CI dependency was deleted.

Separately, the user explicitly approved deletion of the two completed P1
resource groups and accepted loss of fixture databases, disks and blobs
beyond the retained evidence. Fresh checks matched all 505 candidate
identities and tags; the 150-member evidence archive was independently
verified. This archive is not a database, guest-filesystem or blob backup.

Cleanup submitted exactly two parent DELETEs, without retries:

1. `rg-af-vscode-p1-b01-20260921` — 29 retained manifest identities.
2. `rg-af-vscode-p1-20260905-a` — 476 retained manifest identities.

B01's shared-network references were confirmed released before the second
delete. Both parent GETs and all 505 individual identity probes returned 404,
and both filtered inventories were empty. Unlisted provider descendants and
remote content are covered only by parent-cascade semantics; physical data
erasure was not independently certified.

The original nine resources in `rg-agefreighter-cosmos-dev` and ten resources
in `rg-agefreighter-validation-jpe` remain. The latter group's contents were
not classified as disposable. Retained local workflow/report evidence and
the sealed archive are unchanged. Private execution receipts are preserved
outside Git.

## Local worktree cleanup — complete

Six exact sibling worktrees were removed after full backups and independent
verification: `agefreighter-2.3.1`, `agefreighter-age-image`,
`agefreighter-compatibility-matrix`, `agefreighter-horizondb-p2`,
`agefreighter-rc4`, and `agefreighter-v2-release`.

All branch refs remain, including commits not present in cached remote refs.
All working files, 14,154 ignored files, and per-worktree Git metadata were
archived privately. An all-refs/reflogs bundle was independently fetched into a
temporary bare repository and checked for consistency. Before removal, every
archive member's content, mode or symlink target was verified, live inventories
were unchanged and no open files were found. The main checkout and its branch
were preserved. Extended attributes were outside the archive verification.

Private backup manifest SHA-256:
`a72dab2e9bce04adb8782930c832276d198e5fc3ea6eb8e9808a533af260d8a6`.
History bundle SHA-256:
`57c3e6038beba11ac627d9bff86902af392b758f72bc3006bd3aab6e4994a250`.
The archive and actual deletion receipt remain under the ignored
`production-simulation/work/m6-release-20260925/local-cleanup` directory.
No Git branch or remote repository was deleted.

The continuation found no remaining `agefreighter-*` siblings under
`/Users/rifujita/Git_Managed`. One stale Git registration pointed to the already
absent `/private/tmp/agefreighter-dependabot-2`; its ten metadata archive members
were verified before pruning that registration. Its branch and commit
`d3cd90fcea3329e1af63cdd593c379f14fafae36` remain, as does the main checkout.
The three local development databases and separate PostgreSQL 19 test
container were stopped with their volumes preserved; unrelated buildkit was
not stopped or removed.

## Remaining M6 actions

1. Verify public Marketplace 2.4.0 availability and downloaded package contents
   after service verification completes; do not infer success from an upload.
2. Merge the separately tested TCP-readiness and final evidence follow-up
   through normal branch protection, preserving the immutable release tag.
