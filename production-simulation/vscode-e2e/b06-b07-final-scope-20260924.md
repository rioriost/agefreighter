# B06/B07 remaining cases — additional authorized trial

The user authorized an additional **96 hours and USD 400** after the preceding
trial was closed. This approval was first observed at **2026-09-24 12:45:06 UTC**;
the outer trial window ends **September 28 at 12:45:06 UTC**. The first session
reserves only **USD 30**, starts stopping at **230 minutes** after its first VM
intent, and permits at most **240 minutes** of compute, always within the outer
window. These are new bounds for new resources; no old trial is reopened.

September 24 initial checkpoint: **live trial in progress; remaining cases not
yet accepted at that time**. This document preserves the initial plan and its
execution checkpoints; see the [current continuation](b06-b07-continuation-20260925.md)
for later qualification results and cleanup status. Root created
ordinary GUI workflow `8e9f853a-7e9b-4570-8045-952341217524` at
`2026-09-24T12:53:32.851Z`, then entered and reviewed all 18 source mappings in
the normal installed extension. Its reviewed draft has SHA-256
`382758597e6deea205e45bff6436389e1760d12cc85a35123065227b89a68020`.
Compared with the preceding source specification, only the new migration name
and derived target graph differ. B02 and earlier B06/B07 accepted cases remain
credited; the overall ledger at this initial checkpoint was **10/12 PASS**,
before independent review of the remaining cases' actual evidence.

## Exact scope and limits

Subscription `67c417f3-5a13-446c-afb9-40cd87f2fdb7`, existing resource group
`rg-af-vscode-p1-20260905-a`, Japan East zone 1:

| Item | Binding |
|---|---|
| New runner | `af-8e9f853a7e9b45708045`, B2s_v2 then ordinary same-VM D4s_v5 resize, system identity, 64 GiB Standard SSD OS disk, no public IP |
| Runner network | Same-named new NIC/NSG; existing `vnet-af-vscode-p1/subnets/runner` preserved |
| New private target | `afpg-8e9f853a7e9b45708045`, PostgreSQL 18, E8ds_v5 MemoryOptimized, 128 GiB, HA off, public access disabled |
| Target network | New same-named delegated subnet, `10.246.26.0/24`; new `af-8e9f853a7e9b45708045.postgres.database.azure.com` DNS zone and runner VNet link |
| Transfer | New account `af8e9f853a7e9b4570804595`, container `af-8e9f853a-7e9b-4570-8045-952341217524`; exact normal GUI operator Blob Data Contributor and container-scoped new-VM Blob Data Reader |
| Source | Existing `afcosmosp120260907`, `p1/graph`, unchanged generated fixture and all 18 mappings; preserve network, local-key-auth restrictions and throughput |
| Source permission | One normal GUI account-scoped Cosmos Built-in Data Reader grant to the original new VM principal only; actual principal/tenant/role UUID must be verified before dispatch |
| B06 observations | At most two before and three after invocations, each with two IMDS GETs and one `SELECT VALUE 1 FROM c` POST, page size 1, no continuation, at most 60 seconds |
| B07 observations | Exactly one before and one after metadata-only process control around the actual native active-inventory resize refusal, at most 60 seconds each |
| Normal source work | Two complete read-only inventories, each retained and imported through the normal GUI; no migration/load |

Later disk observation: the initial plan and identity projection specified an
SSD. Actual observations immediately before and after the final same-VM resize
instead identified the same 64 GiB `Standard_LRS` HDD. The reason for the
discrepancy with the earlier SSD evidence remains unresolved. The resize
observations establish continuity of that observed disk; they do not establish
that the original SSD plan was maintained
or establish the timing or cause of any earlier storage-type change.

Retain at most 25 live VM command definitions and at most 60 definitions over
the entire workflow lifetime across all sessions. Restarting a session or
retiring a definition does not reset that lifetime count. Commands with unknown
outcomes retain their identity and allow
GET reconciliation only. No automatic request replay, query retry, source write,
worker pause/slowdown, identity rotation or empty-disk fixture is included.
The earlier successful trust/principal/layout cases are not repeated.

Use the previously approved transfer pattern only on the new exact account:
authenticated HTTPS upload, anonymous access and shared keys disabled. If the
same organization policy disables its endpoint, any conditional development
exception is limited to that account, with retained ownership and before/after
evidence. Existing source/network protections and group/subscription policies
remain unchanged. Credentials stay in normal SecretStorage and are never copied
into logs, documents or evidence archives.

## Evidence sequence

1. Seal fresh exact absence, parent-presence, source privacy, shared network,
   full EventGrid baseline, installed artifact and operator-file evidence.
   Bind the guard to this workflow before its first VM intent. Record each new
   resource's exclusive creation marker before its first native creation.
2. Prepare normal transfer storage and the existing pinned good development
   Linux archive, create this runner through the normal GUI and obtain actual
   fresh guest readiness. Verify its sole system principal independently.
3. Run a bounded B06 before observation. Require an actual strict same-message
   principal/action RBAC denial before the native Reader grant; otherwise
   diagnose within the remaining explicit attempt bound. Grant normally, then
   stop after the first matching constant-read success. Do not claim a measured
   propagation delay merely from elapsed wall time.
4. Complete inventory1 and import its sealed full report. Keep a genuinely
   reviewed source panel open before target creation. Provision the normal
   private target and complete a real normal same-VM resize while idle. Refresh
   runner readiness. Separate AGE restart and migration are not needed here.
5. Start inventory2 from the retained reviewed panel. Once the actual worker is
   running and its launch control is finished, cancel only the status watcher.
   Bracket normal **Reconcile resize (read only)** with the two same-process
   observations. Require the actual active-assessment refusal, matching process
   identities, unchanged workflow bytes and no new resize intent. Reconcile and
   import that same inventory2 afterward without replay.
6. Stop both exact compute resources, seal evidence, then clean up only this
   session's newly owned resources/roles/deployments and exact derived children.
   Independently confirm absence and preservation of existing resources and all
   original operator files. A stopped target's unavailable child enumeration
   remains explicit; exact parent deletion does not become individual-child
   inspection evidence.

## Historical preparation evidence — September 24 initial checkpoint

Fresh delayed ActualCost for the two retained trial resource groups at
12:47:24 UTC was **USD 571.42950178288922**. Conservatively adding USD 20 for
unreflected earlier work plus the new USD 30 reserve gives **USD 621.42950178288922**,
within the still-respected USD 800 cumulative ceiling. The new USD 400 allowance
has USD 30 reserved and USD 370 unallocated. This is a delayed billing snapshot
and reservation calculation, not a final invoice or permission to spend the
whole balance. The one query succeeded without a retry.

Read-only precreation checks at 12:58–12:59 UTC confirmed 17 derived identities
absent, the original 25 subnets retained and the proposed prefix nonoverlapping,
source privacy retained, and a complete 26-topic EventGrid baseline with no
new-account derivative. Actual future role UUIDs are not invented or assumed
absent before their native previews exist.

The installed extension remains SHA-256
`7a2aa5ddf13454ff6dab4d4a8ad2cb9a172842c9c4fbcc7b5d8e94d3f79d1d34`.
The good Linux archive remains the previously qualified
`2.4.0-dev.646f0d4b361a` build, SHA-256
`07ba1744b5daf7e226915c50d46fa5eda704949fc7d175e2b39a150813f41692`;
its actual bytes must be rechecked before upload.

The new B06 v4 observer and builder passed 21 author tests plus seven independent
checks. They retain service-derived allowlisted principal/action evidence from
one recognized denial clause, its field path and hashes; status 5302 alone does
not qualify. Observer SHA-256
`d4e7556b0d068c0cb39461988270af2c1c2ef8dae32a93ed16f875afc348fa6c`,
builder `47ba5d65fd0b2a7099847d48c9780f49f2ae7d0247203ff18b3dd4f61bfc4cef`.
The separately adapted stop guard passed 12 offline tests and independent review.
These are preparation results, not live B06/B07 acceptance.

Private preparation/evidence is retained under
`production-simulation/work/b06-b07-final-scope-20260924/`. The preceding
[closed trial](b06-b07-live-20260924.md) and both of its sealed archives remain
unchanged. Exact runtime bindings, first-create timestamps, observation outcomes
and final cleanup receipts will be appended after their actual execution.

## Execution started

Normal GUI created the new transfer storage at 13:04 UTC. Its authenticated
endpoint was enabled using the approved exception limited to this account;
anonymous access and shared keys remained disabled, HTTPS and TLS 1.2 enforced.
An independent GET confirmed these settings. The normal development-artifact
upload reached `ready` with the exact archive hash above.

The exclusive first VM intent was recorded at **13:22:49.071935 UTC** and the
normal GUI creation was submitted once. The running guard fixed its stop at
**17:12:49.071935 UTC** and maximum at **17:22:49.071935 UTC**. Normal GUI
reconciliation confirmed provisioning and successful pinned guest readiness.
An independent exact VM GET confirmed the new sole system identity, expected
OS disk and NIC, and no additional disks. The source Reader grant is still
unsubmitted at this checkpoint; its normal preview was retained before B06.

At 13:34:21 UTC the first before-grant observation returned HTTP 403/5302.
Its supported `Errors[0]` denial clause identified the actual new VM principal
and `Microsoft.DocumentDB/databaseAccounts/sqlDatabases/containers/executeQuery`
as the denied operation. The normal GUI submitted the account-scoped Reader
grant at 13:36:52.270 UTC and reconciled it to `ready`. At 13:39:58 UTC the first
after-grant observation returned HTTP 200 and one constant row. Both were
terminal with exit code zero, the same binding/query/observer hashes, exactly
one query each, and no continuation or retry. Further observations were stopped.
This establishes the strict access-transition branch; B06 still awaits the
normal GUI full inventory and imported report in this trial. The interval is
not a measurement of permission propagation time.

The original helper's CLI entry used the wrong parsed argument attribute and
stopped before dispatch. The unchanged pinned core performed the single
before-grant submission after root's checks; the one-line CLI fix passed 26
tests including real argument handoff and independent review. Both helper
versions and canonical intent records are retained. No request was replayed.
The dispatcher validated the pre-grant role's absence, but its raw GET response
was not archived; the durable admission result is labelled separately from raw
404 evidence.

The first normal full inventory, operation
`0bb518bd-f782-48eb-8234-7128a276443f`, was submitted at 13:41:19.975 UTC.
At 13:55 UTC its ordinary installed-extension watcher had reconciled it to
`finished`, with a 2,940-byte report seal
`5cbe10f11102ff5897ba20098b163d84922ecb8a290e4aa782f5842e84b09e31`.
The Mac locked before the normal report-transfer action. Its full contents and
GUI import remain unverified, so this does not close B06. The user has been
asked to unlock the Mac. The new VM is running under the existing stop guard;
the target DB has not been created. B07 active-inventory qualification and
new-resource cleanup remain pending. The exact resume checkpoint is retained
privately in `execution/resume-after-unlock.json`.
