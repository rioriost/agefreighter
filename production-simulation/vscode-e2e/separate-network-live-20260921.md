# B01 separate migration group / existing network group live trial

September 21, 2026. Status: preparation only; not qualified.

## Authorization and reviewed scope

User renewed a maximum two-hour runtime with the existing USD800 ceiling.
Record the fixed UTC deadline before the first VM creation/start request; do
not restart the clock on resize/reconnect. Stop this trial's runner and target
at that deadline, terminal failure, or fifteen minutes of idle input waiting.
Keep evidence, disks and accepted graphs. Separate action-time gates remain for
new access grants, unpublished executable installation, uploads, public-access
exceptions and any irreversible deletion. No deletion is part of this plan.

Exercise B01 through the installed GUI with a **fresh** workflow:

- Subscription: `67c417f3-5a13-446c-afb9-40cd87f2fdb7`.
- New migration resource group: `rg-af-vscode-p1-b01-20260921`, Japan East.
- Existing source/network group: `rg-af-vscode-p1-20260905-a`.
- Existing VNet/subnet: `vnet-af-vscode-p1/runner`; no peering or source exposure.
- Source: private Cosmos NoSQL `afcosmosp120260907`, database `p1`, container
  `graph-gremlin-p1-20260917`, Gremlin-shaped documents. This is not the Gremlin
  API. Reuse the frozen source, not another workflow's assessment or target.
- New private runner and target belong to the migration group. Target preview
  must put only its new delegated subnet in the original network group, without
  redeploying the existing VNet. Parent and child what-if/operation coverage
  must both pass. Exact generated resource IDs remain pending GUI creation.
- Pinned installed extension SHA
  `68fcf29bf741cde36b028a2a1cfbe39ce40712bac9af1b3dba9ebf79e01c5e40`;
  includes the independent-network-group implementation. Linux candidate
  `d40d6ccc9a4d`, archive SHA
  `2321022975f85c73068a54fd21a287e2802d5bc33fa21eb9323c142dd7262ff6`.

## Fresh read-only gates

- September RG ActualCost query: **USD333.523399055421**, delayed billing,
  not a real-time invoice. Keep a conservative total planning reserve of USD700,
  including the bounded new trial and continuing retention; reject a preview
  that would exceed USD800. No additional budget was authorized.
- Japan East vCPU usage70/101; DSv5 family60/100. Exact B2s_v2 availability,
  VM/DB SKU pricing, target quota and subnet overlap must be checked by fresh
  GUI preflight before deployment. No quota increase is authorized here.
- All13 existing trial VMs are deallocated. Of18 existing Flexible Servers,
  17are Stopped; accepted CSV target `afpg-83c6b829acdc4405aa2d` is Ready/private.
  Its retained workflow is migration finished/qualification pass. Recent activity
  contains failed configuration writes and resource-health events; the cause
  of its running state is not established. Do not claim all old compute stopped
  or interrupt an externally managed workload without resolving ownership.
- Existing group has no returned management lock. Recent activity contains
  storage policy-modify/write events. Do not silently reapply public access or
  security-exception tags; inspect exact new storage state and request its gate.
- New-group name absence and creation, fresh GUI workflow, grants, artifact
  transfer, VM readiness, inventory, target and migration remain pending.

## Acceptance and stopping

First require sealed complete inventory of1.6Mvertices+4Medges/all18labels.
Then require the actual two-scope native approval/what-if, successful exact
parent and child operation coverage, same-VM resize and GUI migration. Final
acceptance requires full64canonical ranges and frozen root, not inventory,
ARM success or counts alone. Active operations are reconciled, never replayed.
If remaining time cannot safely cover the next stage, stop compute and retain
the exact resumable workflow; do not label partial progress B01 PASS.

Before VM start, install a scope-specific safety monitor and fixed shutdown
bound. The monitor must not start/retry jobs or resources. At terminal/bound,
stop only the new trial VM/target and verify states. Existing sources, accepted
targets and old private operator records remain untouched.

## GUI preparation and first approval

Created the named dedicated migration group after verifying name absence; ARM
reports Succeeded with application/owner/purpose/USD800/two-hour-limit tags.
No VNet, peering, VM or target was created as part of group preparation.

The installed GUI selected the private Cosmos candidate from the original
group, then the new migration group, Japan East, explicitly reviewed zone1,
B2s_v2 and the original group's runner subnet. A transient wrong subscription
selection was corrected before selecting any resource or submitting a preview;
no resource write occurred in that subscription. The normal release check
refused the unpublished2.4.0 release, without submitting a runner deployment.
The test-only pinned artifact path remains required; release checks are intact.

Fresh GUI workflow: `5cb990c1-2a25-4de5-a10d-09fab2ef0b18`.
Reviewed source name `p1-b01-gremlin-20260921`, namespace `migration`, source
host/database/container as above, `partitionKey` and
`score=float64,distance_km=float64`. Configuration is read-only inventory-ready;
no assessment has started and no source grant exists yet. All78older operator
files remain byte-identical; only the new workflow file was added.

The user specifically approved transfer account `af5cb990c12a254de5a10d09`
and signed-in-user Blob Data Contributor on that account alone. The installed
native approval submitted `af5cb990c12a254de5a10d09-transfer`; ARM observed
Running at06:25:00UTC and **Succeeded at06:25:28UTC**. Independent ARM reads
confirm the exact signed-in principal's Blob Data Contributor scope is only the
new account. HTTPS-only/TLS1.2, anonymous false and shared-key false are preserved.
Public network access is **Disabled**, despite the reviewed template asking for
Enabled. An early new-group activity read returned no policy-modify event; the
cause is not established and event delivery can lag. GUI retains submitted;
do not claim authenticated Blob access or ready reconciliation yet.

The user subsequently approved this exact account's `SecurityControl=Ignore`
exception, authenticated public HTTPS access and pinned archive upload. Applied
the tag without replacing existing tags, enabled public network access and
independently verified anonymous/shared-key access remained false, HTTPS-only
and TLS1.2 unchanged. Authenticated container listing succeeded; installed GUI
refresh reconciled storage as ready/public Enabled without redeployment.

The installed qualification command reviewed the exact manifest and uploaded
the approved37,197,546-byte archive. Independent Blob properties show creation
at06:31:38UTC, the matching byte length and full SHA256 metadata
`2321022975f85c73068a54fd21a287e2802d5bc33fa21eb9323c142dd7262ff6`.
The local upload record is ready; GUI confirmed the pinned archive is prepared.
This verifies upload/metadata, not a new remote download digest or Linux install.
Actual reconnect restored the same workflow and pinned artifact. Fresh installed
GUI preflight passed at06:33:04UTC: runner `af-5cb990c12a254de5a10d`, B2s_v2,
Japan East/zone1, computeUSD0.109/hour plus storage/network. Preview includes
only the new NSG/NIC/VM and its Blob Data Reader role on this workflow container.
No public IP or source grant is in the template. The preview expires06:48:04UTC
and must be refreshed if approval comes later. Requested separate action-time
approval for VM creation, this identity grant and unpublished installation;
deployment has not been submitted.
No VM start request has been made, so the two-hour compute clock is not running.

All28existing target/template/preflight/lifecycle regression tests pass, including
two-scope what-if, nested operation coverage and denial of unsafe changes. They
remain local tests, not new Azure target-deployment qualification.

Observed wording issue: the discovery page still says the VNet must be in the
migration group, although installed target implementation supports a separate
same-subscription network group. Record this as stale guidance, not a proven
deployment failure. Do not silently loosen actual two-scope admission gates.

## Approved runner deployment

The user approved the exact VM, container-only Blob Reader and pinned executable
installation. Created a thread safety monitor before submission, scoped only to
this runner and any subsequently approved target of this workflow. Hard stop is
**2026-09-21T08:35:00Z (17:35 JST)**, conservatively less than two hours from the
first request; no automatic extension. The monitor must also stop on terminal
failure or fifteen minutes of idle approval waiting. Existing source/accepted
targets remain out of its mutation scope.

Fresh locks returned none and the new group contained only the transfer account.
The installed GUI native confirmation showed the exact VM, B2s_v2/USD0.109 per
hour, pinned SHA and workflow-container reader grant. Accepted that confirmation
after the user approval at06:36:05.759UTC. ARM deployment succeeded06:36:46UTC;
installed GUI reconciled provisioned06:37:13UTC. All VM/NIC/NSG/role operations
succeeded. Exact new VM principal has only the reviewed container Blob Reader;
its NIC uses the existing other-group runner subnet with no public IP. Azure
auto-shutdown is Enabled for08:35UTC on this exact VM, in addition to the monitor.

Initial guest readiness operation `ecfbf845-7ea3-4f31-823f-84d9f76a15b9`, command
`af-67e83d32-1068-47e8-bd32-763bec24d870`, submitted06:37:19UTC and failed
06:37:24UTC with exit127: `/usr/local/bin/agefreighter-tools` did not yet exist.
GUI correctly retained failure rather than accepting ARM provisioning success.
A bounded read-only diagnostic at06:38:54UTC found cloud-init done (finished
06:37:26UTC), the executable now present, root usage4%, used memory275MiB,
no swap and zero matched kernel OOM records. Evidence supports an early-readiness
race with bootstrap completion, not a proven corrupt artifact or permission
failure. No source read or automatic readiness retry was performed.

Per the terminal-failure safety rule, requested deallocation of only this VM
at06:40UTC, preserving OS disk, command record, bootstrap logs and configuration.
ARM verified `PowerState/deallocated` by06:40:15UTC. No source grant, assessment, target or
migration has been submitted. B01 remains partial; the next correction should
distinguish bootstrap-in-progress from terminal guest readiness failure, without
replaying migrations or weakening artifact/health checks. Disabled the scoped
heartbeat after verified deallocation; the Azure VM shutdown schedule remains
enabled as a retained safety setting. No software correction is claimed yet.
