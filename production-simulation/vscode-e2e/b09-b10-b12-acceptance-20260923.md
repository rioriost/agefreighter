# B09 / B10 / B12 — finite acceptance ledger

September 23, 2026. **B10 and B12 pass their defined scopes; B09 remains partial for two real-service failure cases.** This is the authoritative
finite acceptance definition and credit map for this continuation, derived from
the [original branch plan](plan.md#branch-coverage-beyond-the-base-paths) and
[retained branch ledger](remaining-validation.md). Historical execution notes
remain evidence, not new authorization. Update a credit only after reviewing an
actual result; preparation, a planned case or a successful harness exit is not a
case PASS. This document authorizes no installation, authentication change,
compute start, fault injection, source read or deletion.

Current continuation: the [B10 retained-evidence audit](b10-retained-evidence-audit-20260923.md)
independently revalidates its43checks and6negative controls. The [dedicated B09
scope](b09-fault-session-20260923.md) names its prepared negative artifact and
new resource IDs; local preparation is not credit for either pending live gate.

## Evidence layers and credit rules

| Layer | What it establishes |
| --- | --- |
| Preparation / source review | A design or fixture is ready; no execution credit. |
| Local automated | Production logic with inert boundaries; useful regression evidence, not native interaction or Azure behavior. |
| Isolated real host / native | Actual VS Code API, panels or modal interaction with explicitly synthetic prerequisites. Credit only the observed controller/UI boundary. |
| Signed-in installed GUI / real service | The stated normal command and actual service interaction, bound to the retained candidate, account, operation and evidence. |

Generic cancellation can be qualified by an actual native modal and unchanged
production controller with inert effect adapters. Record synthetic selections,
inputs, pricing and ownership replies; do not call this installed-command or live
Azure admission. Require the actual native dismissal, no approved continuation,
no denied effect attempted, and preserved case evidence. If legitimate setup or
read-only reconciliation persists before the modal, retain and validate that exact
initial-to-modal change separately, then require no cancellation-induced change.
Do not hide it by resetting the baseline. Positive approval aborts these fixtures.

## B09 approval cancellations: frozen A01–A30

**30 credited; 0 remaining** after the12:29–12:31UTC remaining17 native batch
and independent12:35UTC file review. The prior13 credits and their evidence are
preserved. Each row is one cancellation decision, not every source, SKU, region
or fixture combination. The mixed layers do not mean30signed-in Azure trials.

| ID | Decision | Credit / retained evidence |
| --- | --- | --- |
| A01 | Create reviewed runner | **PASS**, native Cancel / production controller with synthetic prerequisites; [remaining17 receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A02 | Prepare pinned development artifact | **PASS**, native Cancel / inert file prerequisites; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A03 | Upgrade idle runner | **PASS**, native Cancel / synthetic readiness and inert files; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A04 | Read PostgreSQL catalog | **PASS**, native Cancel / synthetic source; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A05 | Transfer PostgreSQL catalog | **PASS**, native Cancel / synthetic sealed catalog; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A06 | Adopt selected PostgreSQL mappings | **PASS**, native Cancel / synthetic catalog selection; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A07 | Retain failed source assessment | **PASS**, native Cancel / synthetic failure; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A08 | Grant runner Cosmos Reader | **PASS**, native Cancel / synthetic grant preview, no role write; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A09 | Upload reviewed CSV files | **PASS**, signed-in native confirmation Cancel; [CSV transfer](recovery-execution-20260915.md#storage-and-csv-transfer-complete-vm-approval-pending) |
| A10 | Import and seal CSV on runner | **PASS**, native Cancel / synthetic uploaded CSV; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A11 | Create transfer storage / scoped user role | **PASS**, signed-in native Cancel; [GUI preparation](recovery-execution-20260915.md#gui-preparation-after-user-reload) |
| A12 | Retain rejected report export | **PASS**, native Cancel / synthetic rejected transfer; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A13 | Transfer assessment report | **PASS**, native Cancel / synthetic sealed assessment; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A14 | Approve sampled source read | **PASS**, native Cancel after one exact local SourceDraft review; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A15 | Approve complete source inventory | **PASS**, native Cancel after one exact local SourceDraft review; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A16 | Review / approve target deployment | **PASS**, native Cancel before files/resources; [target review](recovery-execution-20260915.md#approved-private-target-submitted), [later repeated cancellation](recovery-execution-20260915.md#approved-target-submitted-through-the-installed-gui) |
| A17 | Repair failed preload setting | **PASS**, native Cancel after one byte-identical failed-status persistence and inert GETs; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A18 | Record new cost authorization | **PASS**, actual native Cancel / production controller with synthetic inputs/pricing; [September23 receipt](evidence/b09-native-cancel-20260923.json) |
| A19 | Apply target preload restart | **PASS**, actual native Cancel / production controller; two inert status GETs, no restart/persistence; [receipt](evidence/b09-native-cancel-20260923.json) |
| A20 | Approve same-VM resize | **PASS**, [signed-in native Cancel](placement-resize-audit-20260917.md#installed-signed-in-gui-observations) |
| A21 | Start new migration | **PASS**, native Cancel / synthetic start prerequisites; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A22 | Explicitly resume retained migration | **PASS**, native Cancel / synthetic resumable operation; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A23 | Inspect retained checkpoint | **PASS**, [signed-in native Cancel](cosmos-guard-gui-20260917.md#actual-signed-in-gui-observations) |
| A24 | Archive empty-target preparation failure | **PASS**, [signed-in native Cancel](cosmos-guard-gui-20260917.md#actual-signed-in-gui-observations) |
| A25 | Diagnose retained target | **PASS**, [signed-in native Cancel](cosmos-guard-gui-20260917.md#actual-signed-in-gui-observations) |
| A26 | Transfer migration verification report | **PASS**, [signed-in native Cancel](cosmos-guard-gui-20260917.md#actual-signed-in-gui-observations) |
| A27 | Run independent P1 verifier | **PASS**, native Cancel with qualification absent and no upload/submission; [PostgreSQL VM record](az-pgvm-execution-20260913.md#safety-and-ui-observations) |
| A28 | Run P1 failure diagnosis | **PASS**, native Cancel / synthetic retained failure and inert file; [receipt](evidence/b09-native-cancel-remaining17-20260923.json) |
| A29 | Archive / remove one readiness control | **PASS for cancellation**, actual native modal with synthetic admission; no archive/intent/removal; [receipt](evidence/b09-native-cancel-20260923.json). Live lifecycle remains separate. |
| A30 | Recover interrupted local lock | **PASS**, actual production command and native Cancel in isolated unsigned-in profile; [local recovery](b10-lock-recovery-20260923.md#actual-native-local-cancellation-and-recovery--09570959-utc), [sealed evidence](evidence/b10-native-lock-20260923.json) |

Remaining cancellation IDs: **none**. The newly completed batch was exactly
**A01–A08, A10, A12–A15, A17, A21–A22, A28**; no accepted case was rerun.
“Save plan only” shares A16's Cancel branch. Corrected P1 requalification shares
A27's exact modal and pre-dispatch Cancel return; neither adds another Cancel
case. Start versus resume and sampled versus complete reads remain distinct.
Existing file/folder/credential-selection and upload-progress cancellations keep
their separate evidence; they do not substitute for these final decisions.

Three earlier setup/test-mode/aborted attempts remain retained without credit,
including the first selector dismissal with `completed: []`. The later normal
development host (not extension-test mode) produced three actual Cancel clicks at
11:39UTC. Independent review matched each result/baseline hash, both current0600
record/report files and the compiled production controller. No fixture effect
was attempted. That checkpoint credited only A18/A19/A29; preparation,
compilation and contract-test success did not promote the then-remaining17.

The later normal-development host completed those17 actual native dismissals
from12:29:58.019UTC through12:31:47.300UTC. Independent12:35:29.996UTC review
matched every initial/modal/result/current file hash and0600 mode, all483 loaded
compiled-module hash entries, controller sources, official executable and the
companion's harness pin. Each case has exactly one native dismissal, no denied
effect and no controller error. A14/A15 each persisted only the exact
production-built SourceDraft before consent; A17 persisted byte-identical failed
status once. Reports were unchanged throughout; all modal-to-final files match.
These pre-consent writes are not hidden by resetting baselines. Root's A01
computer-use pipe error happened after Cancel; the next observed state and sealed
A01 result agree, and A01 was not replayed. The raw fixture ledger's historical
`remainingUncreditedCountBeforeTrial: 20` is not the current acceptance count:
its explicit selected/completed list is17, excluding A18/A19/A29. See the
[new redacted receipt](evidence/b09-native-cancel-remaining17-20260923.json).

## B09 other finite gates

| Gate | Required outcome / current credit |
| --- | --- |
| Expired preview | **PASS for the frozen isolated native/controller scope:** original15-minute review opened before expiry, actual positive returned21.335seconds after expiry and refused before submit/write/effect; original private bytes unchanged. [Expiry receipt](evidence/b09-native-expiry-20260923.json). Prior2failed attempts retain no credit. |
| Duplicate click / multiple windows | **PASS for the frozen isolated native/controller scope:** observed same-window double-click yields one submission; B's held original review in a second real same-profile host refuses after A's single intent. B's native positive return is proven, but deliberate B-button targeting is not. [Receipt](evidence/b09-native-duplicate-20260923.json); no signed-in Azure duplication claim. |
| Lost deployment response | Retain the exact submitted deployment identity / uncertain state and reconcile that operation without PUT replay. **Pending.** B08 lost CSV-upload acknowledgement and lost readiness DELETE replies do not substitute. |
| Terminal bootstrap failure | Actual terminal guest-bootstrap failure remains unready and cannot authorize source dispatch. **Pending.** Historical exit127 during unfinished bootstrap was a transient race, explicitly not this qualification. |
| Missing artifact | **PASS**, actual valid-placement preview refused the unpublished Linux release before deployment; [September22 control](placement-arm-readonly-20260922.md#follow-up-post-fix-valid-placement-control-completed). Do not recreate the failure. |
| Quota denial | **PASS for B09's generic refusal**, installed target preflight rejected regional cores100/101 before target/migration/resize; [retained quota denial](recovery-execution-20260915.md#target-preflight-safely-refused-regional-vcpu-exhaustion). This does not silently close B02's separate remaining cases. |
| Native readiness-control lifecycle | **PASS:** eligible current-boot receipts, actual native Cancel with unchanged record and control, exact user approval, durable4,726-byte v2 archive and intent, native GET-only reconciliation to absent plus independent ARM404. Only the approved old control was removed; current/history/guest evidence retained. [Live receipt](evidence/b09-live-readiness-removal-20260923.json). No instrumented wire request-count claim. |

B09's cancellation decisions are complete; it closes only when its other pending
gates are satisfied.
The coordinator explicitly adopted isolated normal-native / inert-effects proof
for generic expiry and duplicate controller gates: use the corrected production
handler, actual native interaction, real elapsed original expiry and real shared
private store/windows. Record same-window duplicate and two-window cases
separately. This is not evidence of Azure service duplication or signed-in
deployment; actual lost deployment response and terminal guest-bootstrap failure
remain separate real-service/guest gates. Preparation alone closes none of them.
Historical Pending/reference refusal, archive durability, service rejection,
concurrency and crash/lost-ACK regressions retain their documented layers. They
do not require a new live DELETE for every timing. Receipt-specific expiry,
duplicate-window or lost-DELETE trials are optional supplements, not substitutes
for runner-deployment gates. Never reinterpret Pending as success, automatically
replay or delete, adopt legacy controls, or weaken health/archive checks.

### Native duplicate execution — scoped PASS

Independent12:46:33.548UTC review verified both runtime/source pins, physical
profile/store paths, exact original-to-submitted phase/revision delta and the
single inert PUT receipt. Each final store contains one0600 workflow file matching
the retained intent and final hash. The [redacted receipt](evidence/b09-native-duplicate-20260923.json)
records same-window and two-window cases separately.

In the same-window trial, root's actual double-click produced one observed
native modal, one handler entry and one intent/PUT at12:34UTC. This is repeated
UI activation with one submission; it does not claim two native handler entries.
The backend busy-drop test with two injected messages remains local regression
evidence, not newly manufactured native evidence.

The two-window trial used native File > New Window with one disposable profile.
Independent VS Code window1/window2 host logs bind PIDs16461/16672; both actual
host storage URIs and physical stores match. B opened its original review at
12:38:31.516UTC; A returned positive at12:39:27.932UTC and persisted/submitted
once. B's actual native API returned positive at12:39:55.481UTC and refused the
old review at12:39:55.493UTC, with zero submit entries, writes or PUTs. Both
returns precede the original12:52:12.137UTC expiry, so this is not an expiry refusal.

**Input-attribution limitation:** root used window-switch/menu input during lag
and did not knowingly issue a separate B-positive click. The triggering input is
ambiguous. The pinned runtime forwards the real modal API unchanged and never
injects a positive selection; the returned B approval and fail-closed stale-review
boundary are observed. This satisfies the frozen controller gate, but does not
establish deliberately targeted B-button operation or reliable cross-window
computer use. Original runtime files retain their automatic native-qualification
flag as false; independent observations supplement them without rewriting them.
No live Azure duplication, lost-response or bootstrap credit is implied.

The later expiry fixture at `/private/tmp/af-deployment-expiry-ZfIGFf` is
**FAIL / no expiry credit**. Its original expiry was12:47:49.498UTC, but the native
positive returned early at12:35:37.243UTC. It entered submitRunner once, then the
first Azure.runnerRequest was denied by the inert adapter: zero workflow writes,
unchanged private store and no cloud business request. Cloned-application GUI
binding ambiguity is the coordinator's suspected cause, not an established one.
Its failure/result remains intact in the receipt alongside the distinct earlier
pre-modal failure below. A fresh correctly timed trial was still needed at that
checkpoint; the separate successful run is recorded next.

### Native original-expiry execution — scoped PASS

In a fresh single-fixture normal host, the synthetic original preview was created
12:48:13.063UTC with the unchanged15-minute expiry13:03:13.063UTC. Its actual
native modal opened12:49:08.315UTC. Root reports all other fixture applications
closed first, no GUI switching during the hold, a real UTC observation after the
deadline and a fresh accessibility view binding the exact synthetic34975c07
workflow before the positive click. Native positive returned13:03:34.398UTC,
21.335seconds after expiry; the production controller refused the original review
at13:03:34.411UTC before any submitRunner entry, write or effect attempt.

Independent13:05:13.257UTC review verified runtime/loader and all6source seals,
the original exactly900000ms interval, baseline/modal/result/current single0600
record bytes and original phase/revision/expiry. Host logs bind the actual
Extension Host20115. Two read-only restore entries preceded the sole deploy/modal;
there was no second submission. Both prior failure receipts remain byte-identical
and false. See the [redacted expiry receipt](evidence/b09-native-expiry-20260923.json).
This closes the defined expiry controller gate, not signed-in deployment, lost
reply or guest-bootstrap behavior. B09 still requires its real lost-response and
terminal-bootstrap gates; the separate later live readiness lifecycle is credited
in the gate table above.

### Offline native-fixture preparation reviewed

Coordinator verification passed80focused helper tests:51cancellation,
16expiry and13duplicate checks, plus TypeScript type checking. The independent
supervisor found no blocking issue in the unchanged production-controller
expiry/duplicate fixtures. Stable fixture commits are`f82907d` and`86d0c96`.
This adds **no native execution credit**. The normal installed password prompt
was still awaiting private user input, so the coordinator did not switch focus
to a disposable application or start compute.

Subsequently, the first expiry fixture activation at12:28:40.143UTC failed before
any modal because spreading ExtensionContext evaluated the unrelated proposed
`extensionRuntime` getter. Its original failure at
`/private/tmp/af-deployment-expiry-raa5IR/activation-failed.json` is retained;
neither modal nor result exists, so it receives no expiry credit. Both expiry and
duplicate test runtimes now use a lazy context facade with explicit private
storage/secrets overrides. Independent source review approves the fix; the
coordinator's29 combined checks and typecheck pass, including compiled-flow
regressions with an enumerable throwing getter and zero getter reads. No proposed
API was enabled. That preparation alone supplied no expiry credit; only the
separate actual late-confirmation run above closes the gate.

The accepted two-window preparation required launching B after A's baseline and
observing two windows/distinct host PIDs sharing the same physical
profile/store. A reused development host or unavailable concurrent modals is a
blocker, not grounds for substituting another profile. Both native approvals
must finish before original expiry. Same-window proof needs an independently
observed repeated activation and busy/disabled response; the evaluator alone
cannot certify clicks. The preparation bundle mock did not exercise the cross-host
runtime; the actual host evidence above now supplies it. Expiry proof still
requires the actual original15-minute interval,
an early-opened modal and late positive confirmation, with zero submission or
store effects. No clock/timestamp rewrite or Azure qualification is permitted.

## B10: the one required active-cloud crash case — complete

Already credited: [actual Reload Window during assessment, load and verifier](gremlin-gui-execution-20260918.md),
with same-operation completion/canonical PASS; [two isolated real host SIGKILL
boundaries](b10-lock-recovery-20260923.md#real-isolated-extension-host-continuation);
and [native local lock Cancel / explicit recovery](b10-lock-recovery-20260923.md#actual-native-local-cancellation-and-recovery--09570959-utc).
These remain distinct observations, not one combined unobserved end-to-end run.

**PASS September23:** one separately approved, signed-in, genuinely active read-only
inventory crash of the exact Extension Host, followed by reconnect and completion
of that same inventory without resubmission. Bind the active worker, local host
identity, durable operation/configuration/boot, prior assessment history and final
sealed imported report (1.6M vertices,4M edges,18 labels). Recover a real surviving
eligible lock through the native command if present; absence of a lock is valid
and must not be replaced with a fabricated one. Status controls may be new bounded
read-only requests; do not mislabel all follow-up as GET-only. Preserve prior
reports and verify the exact runner/source stopped within approved bounds.

The [live execution](b09-b10-live-20260923.md) now proves the same guest child
active before and after exact host74091's forced exit at13:24:50UTC. The new host
reconnected to inventory401490cc without another inventory approval. It completed
exit0 at13:25:49UTC, and the native GUI imported its sealed2,947-byte report
SHAac52a07c4b43416a954f10c99e528df31455413fdaaac79f810901c2968eb692:
1.6Mvertices/4Medges/all18labels, no errors or incomplete checks. Old report and
history are unchanged. No lock remained, so no live lock recovery is claimed.
Bounded status and export controls are distinct from source re-dispatch;19
managed controls are retained, all Succeeded. Exact runner/source stop requests
were accepted around13:31UTC. Fresh ARM at13:34:49UTC verified runner deallocated
and private source Stopped, before13:50/13:55 bounds; the scoped monitor is paused.
See the [independently reviewed receipt](evidence/b10-live-active-crash-20260923.json).

This one case closes the named remaining gap. Three new forced crashes, another
graph migration and every source/fault timing are **not** required. Exact scope,
capacity, deadline and stop gates are in the [bounded checklist](b09-b10-offline-checklist-20260923.md).
The user has now approved only the exact existing-runner/source60-minute session
recorded in [the live execution log](b09-b10-live-20260923.md), with stop initiation
by minute55 and15-minute idle bound. Approval/preparation is not a test result;
credentials and the live start clock were still pending at that checkpoint.
New resources/source changes and deletion without exact action-time ID approval
remain outside that authority.

The earlier execution log records runner-only start at approximately12:55UTC, with
the source still stopped at that start checkpoint. The absolute stop remains
13:55UTC, stopping to begin13:50UTC. The isolated expiry result does not extend
that active window. The subsequent active-crash evidence above is distinct.

## B12: frozen mixed-layer completion criterion

The original eight negative classes are **count mismatch, digest mismatch,
rejected records, missing evidence, truncated evidence, stale evidence, wrong-job
evidence and incomplete verification**. Existing [counts real-host checks](verification-presentation-20260916.md),
[canonical real-host checks](p1-retained-host-20260922.md) and
[production-controller checks](remaining-next-batch-20260917.md#b12-production-controller-batch-completed)
cover their logic/presentation with synthetic evidence. A [signed-in retained
failure refusal](cosmos-guard-gui-20260917.md#actual-signed-in-gui-observations)
is also credited. Hash-valid rejected canonical bytes remain failure evidence;
reopening rejection need not rewrite historical state, but must create no new
PASS/persistence or automatic download/replay.

**Completed integration:** the normal signed-in native production command refused
wrong-expected-SHA at approximately11:25UTC and wrong-expected-length at
approximately11:36UTC. Each remains unchanged/exported with no retained report or
new PASS. Independent11:43UTC review verified complete case diffs, original
snapshot/guest receipt seals, installed pins and current files. See the
[redacted receipt](evidence/b12-native-download-rejection-20260923.json).

The [fresh-download record](b12-fresh-download-20260923.md) preserves the earlier
authentication blocker. Ordinary interactive sign-in later succeeded; neither
the old auth errors nor setup manifests marked `not-run` are rewritten as report
rejection evidence. No credential-cache copy or security bypass was used.

B12 is **PASS for this defined scope using the explicitly mixed layers above**.
One native qualification action per case was observed; an independent network
request count was not captured. Do not claim all eight classes ran against Azure.
Wrong expected length may fail at Content-Length before reading the body; this
is not observed network truncation. Wrong expected SHA is not remote corruption.
Malformed remote blobs, actual Azure outage and a source/SKU Cartesian product
are optional future work, not additional completion requirements.

## Update discipline

Keep base9/9 and unrelated extended rows unchanged. Credit only named completed
cases with their evidence layer and candidate identity; preserve failures and
empty/aborted runs. Broader tests require an explicit new scope decision, not
silent expansion of this ledger. Time-sensitive runtime and destructive-action
approvals are checked separately at action time and never inferred from this list.
