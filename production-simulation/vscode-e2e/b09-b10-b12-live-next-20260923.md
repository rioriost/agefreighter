# B09 / B10 / B12 next live scopes

September 23, 2026. **B12 zero-compute read-only trial now explicitly authorized;
B09/B10 compute scopes below remain preparation only.** The previous B03 session completed and its resources
were stopped. Installation approval for implementation `3bc0069` does not permit
new compute starts, record deletion, or a signed-in Extension Host crash.

## Read-only prerequisite observations

At approximately 09:50–09:55 UTC, Azure CLI identified the intended enabled
subscription `67c417f3-5a13-446c-afb9-40cd87f2fdb7` and existing signed-in account
`rifujita@microsoft.com`. In `rg-af-vscode-p1-20260905-a`:

- `af-ae9523105eba42b69fe3` is deallocated, Standard_B2s_v2, owned by workflow
  `ae952310-5eba-42b6-9fe3-9db00e93cdac`; 11 managed controls are retained.
- That workflow has a finished PostgreSQL P1 inventory, three sealed readiness
  receipts, no migration, no target, no local lock and no assessment history.
  Its accepted operation `3a4ffae1-8727-464b-8968-40bed2f42c69` must be preserved.
- Its source `afpg-p1-source-20260907` is Stopped. Its old `expiresAt` tag remains
  historical; this observation does not renew a runtime window or modify tags.
- The smaller FK-only alternative `af-24bd714a70ee486582db` is also deallocated,
  Standard_B2s_v2, with 10 controls and no migration/target/lock. Its five-vertex,
  three-edge inventory may finish too quickly to establish an active-worker
  crash, so it is not the default B10 candidate.
- The normal runner store remains 91 files, aggregate path/mode/content SHA-256
  `794d6a7e3c88b12a2b70b0373064bd40f2c746318104d0c82697ea8741551709`.

These are read-only observations, not health checks of stopped guests. Recheck
ownership, governance, cost and operation state immediately before a later
approved mutation. Never restart solely to read old evidence.

## Zero-compute B12 session (separate authorization)

Use a disposable, separately signed-in VS Code profile, the reviewed exact
extension, and only the existing B03 PostgreSQL report:

- workflow `cb2ef280-a891-4edf-b45b-de75e33eb7b8`;
- qualification `028ec524-990f-4ed3-8265-6eb024d5cf57`;
- storage `afcb2ef280a8914edfb45bde`, container
  `af-cb2ef280-a891-4edf-b45b-de75e33eb7b8`;
- original report 23,224 bytes, SHA-256
  `00938de546f94d1fe50e372044bef9b423e104919855fc8d934a06b4b42f77fc`.

After explicit authorization, take a nonsecret record snapshot and independently
retained guest manifest; do not copy SecretStorage or authentication caches.
The reviewed setup helper creates one fresh disposable profile per case. Change
only the copied qualification phase to exported and its expected SHA or length.
Invoke the normal native Continue / Verify command, not a test controller.
Allow genuine ownership reads and just-in-time read-only report capability
issuance, then one bounded fresh Blob GET per case. No VM/database start, upload,
role/network/tag change or accepted-record modification is needed.

Require the expected rejection, no retained report or new PASS, unchanged copied
record, and unchanged original accepted evidence. Wrong Content-Length can be
refused before body read. These are deliberately wrong local expectations, not
Azure corruption or a truncated response. The two cases alone are not all B12:
combine them with retained semantic-invalid production-controller/real-host
coverage, with explicit mixed-layer limits. See
[current B12 acceptance and setup](b12-fresh-download-20260923.md).

## B09 lifecycle session (exact bounded restart and removal approval)

Candidate: only `af-ae9523105eba42b69fe3`; source and all targets remain stopped.
Request a fresh maximum 60-minute window from first start, stop on completion,
terminal failure or 15-minute idle input wait. Reserve the final five minutes
for deallocation. The cumulative USD800 ceiling remains unchanged.

Before start, prepare GUI selection and check cost/governance/control capacity.
After start, require pinned Linux `d40d6ccc9a4d`, idle state, disk below80%, no
swap/OOM and loader RSS no more than4GiB. Do not upgrade software or use a stale
guest health observation. Obtain two naturally successful same-boot readiness
controls through the supported GUI. Preserve historical Pending controls.

The older new control is a candidate only when unreferenced; the separate current
control must be independently matched and younger than five minutes. Observe
native modal Cancel without any archive/intent/delete. Then review the exact
new command ID and obtain the required action-time one-record removal approval.
Retain the normalized archive and intent before the one native DELETE, and later
reconcile absence by GET only. No service acceptance alone counts as success.

If either instance view is Pending/Updating or lacks proof, stop and preserve the
refusal. Do not create retries or weaken admission to manufacture eligibility.
The outcome closes only observed lifecycle subcases, not every B09 approval,
expiry, duplicate-window, lost-reply, bootstrap, artifact or quota branch.

## B10 signed-in active-inventory crash (additional exact approval)

Can share the above runner/window only when separately approved, prerequisites
are ready and sufficient time remains. Also start only source Flexible Server
`afpg-p1-source-20260907`, reuse the existing reviewed 18 P1 mappings and pinned
guest. Prepare the source credential privately before starting compute. Do not
change credentials, roles, schema, data, networking or software. No target or
migration is required.

Submit one explicitly reviewed read-only inventory. Preserve the prior accepted
inventory in assessment history. Record the durable operation/control IDs,
configuration hashes, current boot and actual active-worker evidence. If it has
already finished, do not pretend the crash was during active work or replay it
automatically. Only the specific AGEFreighter Extension Host PID of the approved
test window may be terminated; never the whole user's VS Code application or
another editor/session.

Use the actual native local-lock review/recovery if a same-boot dead-owner lock
survives, with immutable archive and unchanged operation identity. If no lock
survives, report that boundary honestly rather than fabricating one. Reconnect
and reconcile the same remote operation without replaying inventory. A pending
control reconciles by GET; an explicit later status refresh may create a bounded
read-only status Run Command. Do not incorrectly promise every B10 follow-up is
GET-only. Report transfer/import of only this inventory's schema/count evidence
must be included in session authorization.

Require no repeated inventory submission, complete 1.6M vertices plus4M edges
across18 labels, a sealed imported report, and preservation of earlier accepted
evidence. Stop the exact runner and source on completion/failure/idle bound or
the unchanged session deadline. No automatic extension or retry. This is an
inventory-recovery qualification, not another graph migration.

## Coordination and remaining limits

Only root controls Azure and the shared desktop; independent agents review code
and evidence offline. A scoped safety monitor must be configured before a live
start and disabled after independently confirmed stopped states. It may reconcile
and stop only the exact authorized resources, never dispatch trials itself.

Keep base9/9 and extended6pass/6partial until the observed evidence warrants an
individual update. Local native-lock interaction, isolated forced-exit tests and
signed-in active-cloud recovery are distinct evidence and must not be combined
into an unobserved end-to-end claim.
